import React from 'react';
import './header.css'

const Header = () => {
    return (
        <header className="root">
            <h1 className="logo">Twitter</h1>
        </header>
    );
};

export default Header;